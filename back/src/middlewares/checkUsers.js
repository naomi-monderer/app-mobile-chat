/*Sécurité :
- vérifier que rôle est supérieur à 0 si à 0 return (vide en front).
    > check role == 1 else (return void) 
    > Comment récupérer select la table rôle where id = ? 
*/


exports.checkUsers = (res, req, next) => {


}
